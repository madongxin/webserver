# 复制为 config/mysql.cnf 后按环境修改
# cmake -DENABLE_MYSQL=ON . && make
ip=127.0.0.1
port=3306
username=root
password=CHANGE_ME
dbname=metrics
initSize=3
maxSize=32
maxIdleTime=60
maxConnectionTimeQue=3000
