ip=127.0.0.1
port=6379
password=
session_ttl_sec=7200
session_ttl_long_sec=86400
# 断线宽限期（秒）：DISCONNECTED 后可 Reconnect；超时删会话
session_grace_sec=45
# hiredis 连接池大小（SessionService 多线程/多实例共用 Redis）
redis_pool_size=8
# 环境/区服前缀（自动补尾部 :）
key_prefix=gamemesh:dev
